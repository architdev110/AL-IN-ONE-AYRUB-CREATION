import React, { useState } from 'react';\nimport { motion } from 'framer-motion';\nimport { useAppSelector } from '../../hooks/redux';\nimport toast from 'react-hot-toast';\n\nconst Dashboard = () => {\n  const { user } = useAppSelector((state) => state.auth);\n  const [showWelcome] = useState(true);\n\n  return (\n    <div className=\"min-h-screen bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900\">\n      {/* Header */}\n      <motion.header\n        className=\"bg-slate-800/80 backdrop-blur-md border-b border-slate-700 sticky top-0 z-40\"\n        initial={{ opacity: 0, y: -20 }}\n        animate={{ opacity: 1, y: 0 }}\n      >\n        <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6\">\n          <div className=\"flex items-center justify-between\">\n            <h1 className=\"text-3xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-cyan-400\">\n              Dashboard\n            </h1>\n            <div className=\"text-right\">\n              <p className=\"text-slate-300\">Welcome back, {user?.firstName}!</p>\n              <p className=\"text-sm text-slate-500\">Plan: <span className=\"capitalize text-blue-400\">{user?.subscriptionPlan}</span></p>\n            </div>\n          </div>\n        </div>\n      </motion.header>\n\n      {/* Main Content */}\n      <div className=\"max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12\">\n        {/* Welcome Card */}\n        {showWelcome && (\n          <motion.div\n            className=\"bg-gradient-to-r from-blue-600 to-cyan-600 rounded-2xl p-8 mb-8 text-white\"\n            initial={{ opacity: 0, y: 20 }}\n            animate={{ opacity: 1, y: 0 }}\n          >\n            <h2 className=\"text-2xl font-bold mb-2\">Welcome to AYRUB!</h2>\n            <p className=\"mb-4\">Your complete all-in-one platform for professional development</p>\n            <div className=\"flex gap-4 flex-wrap\">\n              <button className=\"px-6 py-2 bg-white text-blue-600 font-semibold rounded-lg hover:shadow-lg transition\">\n                Get Started\n              </button>\n              <button className=\"px-6 py-2 bg-white/20 hover:bg-white/30 rounded-lg font-semibold transition\">\n                Learn More\n              </button>\n            </div>\n          </motion.div>\n        )}\n\n        {/* Quick Stats */}\n        <motion.div\n          className=\"grid grid-cols-1 md:grid-cols-4 gap-6 mb-8\"\n          initial={{ opacity: 0 }}\n          animate={{ opacity: 1 }}\n          transition={{ staggerChildren: 0.1 }}\n        >\n          {[\n            { label: 'Resumes', value: '3', icon: '📄' },\n            { label: 'Portfolios', value: '1', icon: '🎨' },\n            { label: 'Interviews', value: '5', icon: '🎤' },\n            { label: 'Storage Used', value: '2.5GB', icon: '💾' }\n          ].map((stat, i) => (\n            <motion.div\n              key={i}\n              className=\"bg-slate-800 border border-slate-700 rounded-xl p-6 hover:border-blue-500/50 transition\"\n              initial={{ opacity: 0, y: 20 }}\n              animate={{ opacity: 1, y: 0 }}\n              transition={{ delay: i * 0.1 }}\n            >\n              <div className=\"text-3xl mb-2\">{stat.icon}</div>\n              <p className=\"text-slate-400 text-sm mb-2\">{stat.label}</p>\n              <p className=\"text-2xl font-bold text-white\">{stat.value}</p>\n            </motion.div>\n          ))}\n        </motion.div>\n\n        {/* Tools Section */}\n        <motion.div\n          initial={{ opacity: 0 }}\n          animate={{ opacity: 1 }}\n          transition={{ delay: 0.5 }}\n        >\n          <h2 className=\"text-2xl font-bold text-white mb-6\">Popular Tools</h2>\n          <div className=\"grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-4\">\n            {[\n              { name: 'Resume Builder', icon: '📝', color: 'from-blue-500 to-blue-600' },\n              { name: 'Portfolio', icon: '🎨', color: 'from-purple-500 to-purple-600' },\n              { name: 'Interview Prep', icon: '🎤', color: 'from-green-500 to-green-600' },\n              { name: 'Cover Letter', icon: '✉️', color: 'from-orange-500 to-orange-600' },\n              { name: 'Invoice Generator', icon: '💰', color: 'from-yellow-500 to-yellow-600' },\n              { name: 'PDF Toolkit', icon: '📋', color: 'from-red-500 to-red-600' },\n              { name: 'QR Generator', icon: '📱', color: 'from-cyan-500 to-cyan-600' },\n              { name: 'File Sharing', icon: '📁', color: 'from-indigo-500 to-indigo-600' }\n            ].map((tool, i) => (\n              <motion.button\n                key={i}\n                className={`bg-gradient-to-br ${tool.color} rounded-lg p-6 text-white font-semibold hover:shadow-lg hover:shadow-${tool.color.split(' ')[1]}/50 transition group text-center`}\n                initial={{ opacity: 0, scale: 0.95 }}\n                animate={{ opacity: 1, scale: 1 }}\n                transition={{ delay: i * 0.05 }}\n                whileHover={{ scale: 1.05 }}\n              >\n                <div className=\"text-4xl mb-2 group-hover:scale-125 transition\">{tool.icon}</div>\n                <p className=\"text-sm\">{tool.name}</p>\n              </motion.button>\n            ))}\n          </div>\n        </motion.div>\n      </div>\n    </div>\n  );\n};\n\nexport default Dashboard;
